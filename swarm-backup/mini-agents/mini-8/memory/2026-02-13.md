# Capabilities Summary

- **Workspace**: `/home/pmello/.openclaw/workspace-mini-8`
- **Files read**: SOUL.md, IDENTITY.md, USER.md, AGENTS.md, MEMORY.md, TOOLS.md
- **Commands available**:
  - `ls` lists directory contents.
  - `which python3` shows python executable location (`/home/linuxbrew/.linuxbrew/bin/python3`).
  - `which node` shows node executable location (`/usr/bin/node`).
  - `which git` shows git executable location (`/usr/bin/git`).
- **Tool environment**: sandbox host, allowlist security.
- **Next steps**: Build a simple Python script tool and document.
